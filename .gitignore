node_modules/
.next/
out/
build/
dist/
.env
.env*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts

# Single-lockfile policy: pnpm-lock.yaml is canonical (matches the Vercel build + base-ui 1.5.0).
# npm's package-lock.json is intentionally untracked so Vercel's package-manager inference
# stays deterministic (sole lockfile → pnpm). Run `npm install` locally if pnpm is unavailable.
package-lock.json
.DS_Store
.idea/
.vscode/
playwright-report/
test-results/
*.log
inbox.html
pst3.html
reader.html
posteingang-*.txt
posteingang-*.html

# Local explorative scripts + lighthouse/probe artifacts (do not commit)
.contrast-probe.mjs
.rtl-probe*.mjs
.lh-*.json
.lh-*.html
.lh-persona-runner*.mjs
layout.css
response.html

# Local generated artifacts (do not commit)
.codegraph/
.audit-shots/
shots/

# Isolated Next build dirs (NEXT_DIST_DIR — prod-gate runs beside the live dev .next)
.next-turbo/
.next-prod/
.next-orch/
.next-e2e-dash/

# Demo-video takes (regenerate with `npm run demo:record`)
demo-recording/

# Internal working material — local pipeline only, NOT part of the public artefact.
# docs/design-prototype/ (README screenshots) + docs/architecture.md (README-verlinkt)
# stay tracked; everything else under docs/ is local.
/docs/*
!/docs/design-prototype
!/docs/design-prototype/**
!/docs/architecture.md
/CLAUDE.md
/.claude/
/src/lib/i18n/_status.json
/BACKLOG.md
/routine-backlog-scout.md
/update-log.md
/.mcp.json
/design-handoff/
/remotion/

# Demo-video capture pipeline + one-off probe/shot tooling — local only.
# (Protocol scripts scripts/eudi-*, fc-*, once-only-* stay tracked — README documents them.)
/tests/demo/
/tests/e2e/_geburt-shot.spec.ts
/tests/e2e/_spread-spotlight-probe.spec.ts
playwright.*.config.ts
/scripts/capture-lg-reflow-smooth.mjs
/scripts/dark-shot-one.mjs
/scripts/dark-shots.mjs
/scripts/generate-teaser.mjs
/scripts/render-demo.mjs
/scripts/screenshot-all.mjs
/scripts/shot-familie.mjs
/scripts/shot-vg.mjs
/scripts/teaser-test.mjs
/scripts/warm-dev-routes.sh
/scripts/install_pkgs.sh
/scripts/record-rathaus-60.mjs

# Personal presentation materials (Rathaus-Termin) — local only.
/presentation/

# One-off local probe/screenshot scripts + their image output
tmp-*
/scripts/_*-tmp.mjs
.tmp-shots/
.dark-shots/
.ds-sync/
ds-bundle/
/.design-sync/

# FIT-Connect local private keys (Protokoll-Modus)
.secrets/

# Third-party tooling clones — own repo/history, not part of govtech.
/tools/
